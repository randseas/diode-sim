import { Element } from "@/types/elements";
import { GENERIC_COMPONENTS } from "@/constants/elements/components";
import { GENERIC_SOURCES } from "@/constants/elements/sources";

export const GENERIC_ELEMENTS: Record<string, Element[]> = {
  components: GENERIC_COMPONENTS,
  sources: GENERIC_SOURCES,
};
